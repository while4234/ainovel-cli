# Git History

## Repository Policy

- Local Git is used for rollback, review, and bug investigation.
- Secret files such as `.env`, keys, and credential files are intentionally ignored.
- Remote pushes require explicit user instruction or a project-specific opt-in.

## Quick Commands

- Status: `git status --short`
- Recent history: `git log --oneline --decorate -n 20`
- Inspect a commit: `git show <hash>`
- Restore a file from a commit: `git restore --source <hash> -- <path>`

## Change Log

| Date | Commit message | Type | Files | Validation | Notes |
| --- | --- | --- | --- | --- | --- |
| 2026-07-08 | fix: complete repaired outline rewrites | fix | Resume repair finalization, pending rewrite word-count reconciliation, repaired chapter fact cleanup/recreate, repaired arc postprocess queue, commit/rewrite flow tests | Focused store repair/reconcile tests passed; focused tools commit rewrite-fact tests passed; focused host/flow and host resume tests passed. `go test ./internal/store ./internal/tools ./internal/host/flow -count=1` passed for tools and host/flow, while store package reruns hit only known Windows `TempDir RemoveAll cleanup` / `unlinkat directory is not empty` flakes, with affected focused tests passing on rerun. `git diff --check` passed with CRLF warnings only. | Resume now completes any interrupted outline-repair finalization before honoring a clean scan marker, reconciles stale word counts for deleted pending rewrites, deletes old runtime facts and cast appearances for repaired chapters, recreates those facts when the repair rewrite commits a deleted final chapter, and queues repaired completed arcs for review/summary before normal writing continues. |
| 2026-07-08 | fix: review duplicate outline batches by similarity | fix | Outline duplicate scoring, batch-scoped resume repair marker, arc repair cleanup/rewrite queue, planner detail duplicate review, `save_foundation` similarity review, Architect prompt, regression tests | `go test ./internal/domain ./internal/tools ./internal/host/flow -count=1` passed on rerun; `go test ./internal/host/flow -count=1` passed; real project scan covered `jqmq1_3`, `冒险屋`, `诡案组`, and `理化课`; broader `internal/store`, `internal/host`, and `internal/host/adapt` runs hit only known Windows `TempDir RemoveAll cleanup` / `unlinkat directory is not empty` flakes after assertion failures were fixed. | Duplicate outline detection now treats exact signatures, identical long titles, high detail/full similarity as automatic duplicates, and sends 0.70+ borderline pairs to model review. Resume scans only within generated arc batches and writes a clean scan marker so unchanged projects are not reanalyzed. Repairing a batch deletes stale article artifacts for that batch and queues only completed repaired chapters for rewrite. |
| 2026-07-08 | fix: restore project runtime fallback switching | fix | Host runtime fallback config snapshot, runtime fallback terminal retryability, planner retry regression tests, project-wide model switch regression tests | Focused `go test ./internal/bootstrap -run "TestRuntimeAutoSwitch(GatewayErrorSwitchesImmediately\|ExhaustedGatewayErrorIsRetryable\|ExhaustedRateLimitExceededIsNotRetryable\|ExhaustedErrorIsNotRetryable\|RateLimitExceededSwitchesImmediately\|RateLimitExceededWalksDeepseekCandidatePool)" -count=1`, `go test ./internal/host -run "Test(SelectRuntimeFallbackPromotesQuotaTargetToAllAgents\|ReportAdaptationFailoverPromotesTargetToAllAgents)" -count=1`, and `go test ./internal/host/adapt -run "TestShouldRetryPlannerGenerate" -count=1` passed; `go test ./internal/bootstrap ./internal/host ./internal/host/adapt -count=1` passed for bootstrap/host and hit known Windows `TempDir RemoveAll cleanup` flakes in adapt, with affected tests passing on focused rerun; `go build -o "$env:TEMP\ainovel-cli-check.exe" ./cmd/ainovel-cli` passed. | Host runtime fallback now snapshots config with project overlay flags intact, so quota/rate-limit/gateway errors can actually enter the project auto-switch pool and promote the selected fallback model to default, coordinator, architect, writer, and editor. Runtime fallback terminal errors now remain non-retryable for hard quota/rate-limit exhaustion such as `rate_limit_exceeded`, but temporary gateway/network/timeout failures such as `503 Service Unavailable` can still use the planner's outer retry budget after switch candidates are exhausted. |
| 2026-07-08 | fix: switch all agents on model limits | fix | Adaptation planner retry guard, Host adaptation failover promotion, regression tests, rebuilt CLI executable | `go test ./internal/host/adapt -run "TestShouldRetryPlannerGenerate" -count=1` passed; `go test ./internal/host -run "TestReportAdaptationFailoverPromotesTargetToAllAgents" -count=1` passed; `go test ./internal/bootstrap ./internal/host ./internal/host/adapt -count=1` passed for bootstrap/host and hit known Windows `TempDir RemoveAll cleanup` / `unlinkat directory is not empty` flakes in `internal/host/adapt`; `go build -o ainovel-cli.exe ./cmd/ainovel-cli` passed. | `rate_limit_exceeded` and quota-like hard-limit errors no longer burn the adaptation planner's outer retry budget after runtime fallback is exhausted. Explicit adaptation failover now promotes the fallback target through the default model switch path, so coordinator, architect, writer, and editor all move to the same provider/model and the route is persisted. |
| 2026-07-08 | fix: restore web project creation | fix | Web project create handler, project-create UI regression test, rebuilt embedded static assets, GIT_HISTORY | `npm.cmd --prefix internal\entry\web\ui test -- --run` passed; `npm.cmd --prefix internal\entry\web\ui run build` passed; `go test ./internal/entry/web -count=1` passed; `go build -o .\ainovel-cli.exe .\cmd\ainovel-cli` passed. | The left-pane plus button no longer reads the unrelated undefined `budgetAttempts` retry setting before calling `/api/projects`, so creating/opening a fresh novel reaches the backend again. The embedded Web bundle was rebuilt so production Web serves the fixed JavaScript. |
| 2026-07-08 | fix: repair duplicate outlines on resume | fix | Generic outline duplicate detection, `repair_arc` foundation tool/store repair path, resume preflight queueing, flow router repair dispatch, adaptation plan sync, regression tests, GIT_HISTORY | `go test ./internal/domain ./internal/store ./internal/tools ./internal/host/flow ./internal/host -count=1` passed for domain/store/tools/flow and failed only on `internal/host` suite-level timing in `TestHostCloseWhileWaitDonePendingDoesNotPanic`; focused reruns for the two new resume tests and that close test passed; `go test ./internal/tools -run TestSaveFoundationRepairArc -count=1` passed. | Resume now scans saved chapter outlines in code before prompting the model. If it finds duplicate title/core_event/hook promises, it routes `architect_long` to `save_foundation(type="repair_arc")` for the later duplicate's expanded arc; already-completed chapters in that arc are merged into the rewrite queue and flow is upgraded to rewriting before writer resumes. In adaptation projects the confirmed plan's outline fields are synchronized while source anchors and word budgets are preserved. |
| 2026-07-08 | fix: reject duplicate adaptation outlines | fix | Adaptation outline duplicate signature helper, planner proposal/detail validation, writer chapter guard, regression tests, GIT_HISTORY | `go test ./internal/domain ./internal/tools -count=1` passed; `go test ./internal/host/adapt -run TestPlannerBatchChapterValidator -count=1` passed; focused duplicate guard tests passed; full `go test ./internal/host/adapt -count=1` and `-parallel=1` hit only known Windows `TempDir RemoveAll cleanup` flakes, with failed tests passing when rerun individually. | Confirmed the `女魔头` ch14/ch18 issue is a real saved adaptation-plan duplication: the model repeated the same title, core event, and hook, and the host accepted it. Planner detail/final validation now rejects repeated story promises so repair can regenerate them, and writer tools block confirmed plans that still duplicate an earlier chapter before drafting. |
| 2026-07-08 | fix: stabilize web static build output | fix | Vite static output line-ending normalizer, tracked static index baseline, `.gitattributes` LF rule, Git history | `npm.cmd --prefix internal\entry\web\ui test -- --run` passed; `npm.cmd --prefix internal\entry\web\ui run build` passed; `git diff --check -- internal/entry/web/ui/vite.config.js internal/entry/web/static/index.html` passed with CRLF warning only before the LF rule update. | Web UI production builds now normalize generated `.html`, `.css`, and `.js` files to LF after Vite writes them, and the tracked `static/index.html` matches Vite's stable generated output. This prevents `restart-web.cmd` from leaving `internal/entry/web/static/index.html` dirty due line-ending churn after a restart. |
| 2026-07-08 | fix: allow free source-map range sharing | fix | Free adaptation source-map skeleton normalization, skeleton repair prompts, free/arc source-range regression tests | Focused free/arc skeleton/detail tests passed; `go test ./internal/host/adapt -run "TestNormalizePlannerSourceMapSkeletonBatches\|TestBuildAdaptationPlannerSkeletonUserPrompt\|TestRepairPlannerSkeletonText\|TestPlannerBatchChapterValidatorFreeKeepsSharedRangeWithoutArcBudgetSplit\|TestBuildAdaptationProposalUsesParentSourceRangeForChunkedFinalCoverage\|TestValidatePlannerProposalFreeAllowsCompressedSourceRangeButCapsTargetChapterBudget\|TestBuildAdaptationProposalDetailsSplitsLongSourceChapterBudget" -count=1 -v` passed; `go test ./internal/tools -count=1` passed; `git diff --check` passed with CRLF warnings only; full `go test ./internal/host/adapt -count=1` hit only known Windows TempDir cleanup flakes, and the reported tests passed when rerun individually. | `free/full_rewrite` source-map skeleton batches may now share or overlap source ranges within the current fixed source-map entry, so repeated single-source ranges such as `140-140` no longer exhaust structure repair with `does not advance`. Reasonable model-provided subranges are preserved when they stay inside the entry, while `arc/full_rewrite` keeps strict partition checks. Detail chapter planning remains no stricter than arc on structural bounds, and final proposal normalization still caps target chapter budgets. |
| 2026-07-08 | fix: soften free adaptation source budgets | fix | Free/arc adaptation planner source-rune split policy, planner prompts, writer adaptation prompt, free/arc budget regression tests | Focused free/arc budget tests passed; `go test ./internal/host/adapt -run "TestNormalizePlannerSourceMapSkeletonBatches\|TestBuildAdaptationPlannerSkeletonUserPrompt\|TestValidatePlannerProposalFreeAllowsCompressedSourceRangeButCapsTargetChapterBudget\|TestBuildAdaptationProposalDetailsSplitsLongSourceChapterBudget\|TestBuildAdaptationProposalDetailsUsesCompactBudgetRepairPrompt" -count=1 -v` passed; `go test ./internal/tools -count=1` passed; `git diff --check` passed with CRLF warnings only; `go test ./... -count=1` passed for changed packages including `internal/host/adapt` and failed only on known Windows `internal/store` TempDir cleanup, with `go test ./internal/store -run TestSetPendingRewrites -count=1 -v` passing on rerun. | `free/full_rewrite` now treats `source_runes` as density/context guidance rather than a minimum target chapter count, so one long original chapter can be compressed or restructured without budget quality retries. The per-target chapter guard remains: `chapter_budget_policy` is still present for free and oversized `word_budget.max_runes` values are normalized back to the 5000 model chapter cap. `arc/full_rewrite` keeps the old source-rune split enforcement. |
| 2026-07-08 | fix: bound long novel context packets | fix | Windowed `novel_context` chapter packets, `outline_range` scope, compact context helpers, Writer/Architect prompt policy, longform regression tests | `go test ./internal/tools ./internal/agents/ctxpack ./internal/domain` passed; `go test ./...` passed; real `jqmq1_3` chapter 7 probe returned 59,965 bytes with windowed outline chapters 5-10, total chapters 117, and no `_trimmed`; `.\restart-web.cmd -Port 9898 -RuntimeRoot C:\Users\Hi\.ainovel\novels-preview` passed and restarted Web at `http://127.0.0.1:9898` PID 3408. | Writer context no longer loads the full long outline or full growing ledgers. Quality-critical current chapter, nearby outline, current arc, recent summaries, character/foreshadow state, source reports, rules, and references are selected at source, while distant outline spans are available through `novel_context(scope="outline_range", from=X, to=Y)`. Hard post-build trimming was removed. |
| 2026-07-08 | fix: repair adaptation budget planning | fix | Adaptation planner shared source-range validation, volume budget-quality repair retries, budget-quality retry config/API/UI, rebuilt Web static assets, secret-safe `.gitignore` rules | `go test ./internal/bootstrap ./internal/domain -count=1` passed; `go test ./internal/entry/web -count=1` passed; `go test ./internal/entry/web -run TestProjectRetrySettings -count=1 -v` passed; `go test ./internal/host/adapt -run TestPlannerBatchChapterValidator -count=1 -v` passed; focused accepted-deviation, budget-quality retry, compact budget-repair prompt, and budget repair instruction tests passed; `go test ./internal/host -run TestSnapshotUsesAdaptationProposalForChapterVisibility -count=1 -v` passed; `npm.cmd --prefix internal/entry/web/ui test -- --run` passed; `npm.cmd --prefix internal/entry/web/ui run build` passed; `go build -o .\ainovel-cli.exe .\cmd\ainovel-cli` passed; `git diff --check` passed with CRLF warnings only. Broader `internal/host` and `internal/host/adapt` package runs still hit known Windows `TempDir RemoveAll cleanup` flakes, and the affected tests passed when rerun directly. | Full-rewrite chapter details may now share a broad parent `source_range` while per-chapter budgets stay split. Oversized volume source ranges are repaired with a compact budget prompt before detail generation, governed by the new `budget_quality_max_attempts` setting exposed through config, Web APIs, and UI. Accepted compressed ranges are recorded with planner notes to avoid repeated budget failures, and generated static assets were rebuilt from the updated UI. |
| 2026-07-07 | fix: surface retry settings persist failures | fix | Host retry-settings persistence, Web project retry regression tests, GIT_HISTORY, handoff | `go test ./internal/entry/web -run "TestProjectRetrySettings(PersistAcrossReopen\|ReturnsPersistError)$" -count=1 -v` passed; `go test ./internal/entry/web -count=1` passed; `go test ./internal/host -run "Test.*Model\|Test.*Retry\|Test.*Provider\|Test.*CoCreate" -count=1` passed; focused related Web project tests passed; `git diff --check -- internal/host/host.go internal/entry/web/projects_test.go` passed with CRLF warnings only; `.\restart-web.cmd -Port 9898 -RuntimeRoot C:\Users\Hi\.ainovel\novels-preview` passed and restarted Web. | Project retry settings no longer report success when writing the project overlay/global config fails. `SetRetrySettings` now rolls back in-memory retry values and returns the persistence error, so the UI can surface a real save failure instead of showing a value that disappears after restart. A live temporary-project smoke test confirmed 14/14 persisted to the project config file. |
| 2026-07-07 | fix: tighten adaptation proposal retry validation | fix | Adaptation proposal planner detail validation, source-map/detail repair prompts, runtime budget-split retry cleanup, focused regression tests, handoff | Focused `go test ./internal/host/adapt -run <planner budget/source-map retry tests> -count=1` passed; `git diff --check` passed with CRLF warnings only; full `go test ./internal/host/adapt -count=1` and focused `TestBuildAdaptationProposalRejectsInvalidPlannerWordBudgets` rerun hit only Windows `TempDir RemoveAll cleanup: directory is not empty` under `meta/adaptation` after assertions. | No hard fallback or auto-correction was added. Detail generation now reviews completed parent source ranges before reuse, validates source-range budget splitting as soon as a parent skeleton batch closes, aggregates all budget-invalid source ranges, and removes every completed detail batch covering those ranges so retries regenerate the real failed areas instead of exposing one new bad range per round. Prompts now clarify that `source_from/source_to` are model-owned source coverage and that `source_range` budget errors must be fixed by redistribution, not by lowering reported rune budgets. |
| 2026-07-07 | fix: switch runtime fallback on usage limits | fix | Runtime fallback quota-error classifier, monthly usage-limit regression test, GIT_HISTORY, handoff | `go test ./internal/bootstrap -run TestRuntimeAutoSwitch -count=1` passed; `go test ./internal/bootstrap -count=1` passed; `git diff --check` passed with CRLF warnings only; `.\restart-web.cmd -Port 9898` passed and restarted Web on `http://127.0.0.1:9898` PID `35872`. | Provider messages such as `Monthly usage limit reached. Resets in 18 days` now classify as `quota_exhausted`, so runtime auto-switch and explicit role fallback can move to the next configured provider/model instead of surfacing the planner batch error immediately. |
| 2026-07-07 | fix: resume adaptation proposal details after validation errors | fix | Adaptation proposal planner runtime reuse/final-validation handling, budget-split error typing, runtime resume regression tests | `go test ./internal/host/adapt -run "TestBuildAdaptationProposal(KeepsChunkedRuntimeAfterFinalValidationFailure\|DropsBudgetInvalidRuntimeSourceRange\|ResumesChunkedPlannerRuntimeAfterBatchFailure)$" -count=1` passed; `go test ./internal/host/adapt -count=1` passed; `go test ./internal/host/imp -count=1` passed; `go test ./...` completed with only known Windows `TempDir RemoveAll cleanup` failures in unrelated packages; `.\restart-web.cmd` passed and restarted Web on `http://127.0.0.1:9898` PID `24064`; HTTP `/` returned 200. | Chapter-detail proposal checkpoints are no longer cleared after final validation failures. Reused detail batches are revalidated before reuse, invalid cached batches are discarded and regenerated, and budget-split validation errors remove completed batches covering the offending source range so retry resumes near the failed range instead of starting at batch 1. |
| 2026-07-07 | fix: budget long-source adaptation splits | fix | Adaptation model chapter budget constants, planner source-map/detail budget policy, store load-time split-budget normalization, regression tests | `go test ./internal/store -run "TestAdaptationPlanLoadNormalizesSplitFullRewriteBudgets\|TestAdaptationPlanPersistsSourceDerivedSoftBudgets\|TestAdaptationPlanLoadNormalizesLegacyAndNestedWordBudget" -count=1` passed; `go test ./internal/host/adapt -run "TestBuildAdaptationProposalDetailsSplitsLongSourceChapterBudget\|TestNormalizePlannerSourceMapSkeletonBatchesRequiresBudgetSplitForLongSourceChapter\|TestBuildAdaptationProposalFillsMissingChapterWordBudget\|TestBuildAdaptationProposalRejectsInvalidPlannerWordBudgets" -count=1` passed; `go test ./internal/domain -count=1` passed; `go test ./internal/tools -count=1` passed; `git diff --check` passed with CRLF warnings only; `go build -o .\ainovel-cli.exe .\cmd\ainovel-cli` passed; `restart-web.cmd -Port 9898 -RuntimeRoot C:\Users\Hi\.ainovel\novels-preview -NoBuild` passed and started PID `22548`; `/api/runtime` returned healthy. Full `go test ./internal/store -count=1` and `go test ./internal/host/adapt -count=1` hit only Windows `TempDir RemoveAll cleanup: directory is not empty` cleanup flakes after assertions. | Arc/free planner source-map entries now carry source rune counts, skeleton validation requires enough target chapters for long source ranges, and final proposal validation redistributes oversized per-chapter budgets across split target chapters so a 16k source chapter split into four target chapters receives about 4k per target instead of 14k-18k. Existing saved full-rewrite plans are also normalized on load when the same source coverage was already split into enough target chapters, so resumed projects no longer keep stale per-target 14k-18k budgets. |
| 2026-07-06 | fix: switch runtime fallback on invalid tokens | fix | Runtime fallback auth-error classifier, runtime fallback regression test, GIT_HISTORY | `go test ./internal/bootstrap -run TestRuntimeAutoSwitch -count=1` passed; `go test ./internal/bootstrap -count=1` passed; Web restart intentionally not run per request. | Bare provider errors such as `Invalid token (...)` and `authorization failed` now enter runtime model auto-switch as `auth_failed` instead of being treated as protected unclassified errors, so adaptation planner calls can switch to configured candidate models. |
| 2026-07-06 | fix: prevent adaptation mode scroll jumps | fix | Web UI side-pane/adaptation choice CSS, style tests, rebuilt static Web assets | `npm.cmd --prefix web test` passed; `npm.cmd --prefix web run build` passed; `go test ./internal/entry/web` passed. | Hidden radio inputs for adaptation/co-create choices are now anchored inside their visible labels so focusing `chapter`/`arc`/`free` cannot scroll the document unexpectedly. Short desktop viewports also use tighter pane spacing and internal side-pane scroll containment for 1080p displays with scaling. |
| 2026-07-06 | fix: split simulation retry budgets | fix | Simulation structured JSON retry policy, Web simulation host deps, event-feed error detail display, rebuilt static assets, regression tests | `go test ./internal/host/sim -run "TestStructuredJSONCall\|TestRunnerPersistsAnalyzedSourcesBeforeFailureAndResumes\|TestImportProfile" -count=1` passed; `go test ./internal/host/sim ./internal/host -count=1` passed; `go test ./internal/entry/web -count=1` passed; `npm.cmd --prefix internal/entry/web/ui test -- --run` passed; `npm.cmd --prefix internal/entry/web/ui run build` passed; `git diff --check` passed with CRLF warnings only; `restart-web.cmd -Port 9898 -RuntimeRoot C:\Users\RondleLiu\.ainovel\novels-preview` passed after prepending repo-local Go to PATH and restarted PID `55452`; HTTP `/api/runtime` returned healthy with 19 projects. | 仿写语料分析、画像合成、画像导入后的重合成 now use separate retry budgets: transient model-call failures use `model_call_max_attempts`, while invalid structured JSON uses `structure_repair_max_attempts`, and each step/request counts its own attempts independently. SIMULATE retry events now distinguish model-call retry from structure repair, and the main event feed shows host-event `detail` for error/warn rows so failures no longer collapse to only `语料分析失败`. |
| 2026-07-06 | fix: keep adaptation cocreate brief cancellable | fix | Adapt co-create draft prompt/token defaults, adapt decision draft repair instructions, target chapter-count inference, Web co-create cancel UI/backend/static assets, regression tests | `go test ./internal/entry/web -run TestProjectSessionAdaptCoCreateCommitCanBeCanceledFromCoCreateCancel -count=1` passed; `go test ./internal/entry/web -count=1` passed; `go test ./internal/host -run TestAdaptCoCreatePromptKeepsDraftBriefLevel -count=1` passed; `go test ./internal/host/adapt -run TestInferTargetChapterCountFromBrief -count=1` passed; `npm.cmd --prefix internal\entry\web\ui test -- --run src/workspace-progress.test.js` passed; `npm.cmd --prefix internal\entry\web\ui run build` passed; `git diff --check` passed with CRLF warnings only. Full `go test ./internal/host` and `go test ./internal/host/adapt` package reruns hit only known Windows `TempDir RemoveAll cleanup: directory is not empty` flakes; the reported tests passed when run directly. | Adapt co-create drafts are now instructed to stay brief-level instead of generating chapter-by-chapter strategy, default co-create max tokens drop from 8192 to 4096, source chapter anchors such as `第290-291章` / `ch290-291章` no longer infer a 291-chapter target, and cancel remains available during adaptation proposal planning without exposing unrelated busy actions to accidental cancellation. |
| 2026-07-06 | fix: avoid false co-create failure state | fix | Web adaptation co-create checkpoint state, session checkpoint writer, in-progress briefing regression test, fake Web host test helper | `go test ./internal/entry/web -run TestProjectAdaptCoCreateBeginDoesNotExposeRecoverableCheckpointAsFailure -count=1` passed; `go test ./internal/entry/web -run "TestProjectAdaptCoCreate(BeginWaitsForBriefingDecision\|ResumeRestoresFailedBriefingGeneration\|ResumePreservesFailedDecisionProgress)" -count=1` passed; `go test ./internal/entry/web -count=1` passed; `git diff --check` passed with CRLF warnings only. | Adapt co-create no longer marks the live/API session as failed while the first briefing is still generating. The initial checkpoint is still written as recoverable for crash/restart safety, but the in-memory state only exposes `failed=true` after a real briefing or model error. |
| 2026-07-06 | fix: honor planner skeleton repair budget | fix | Adaptation planner skeleton/detail repair retry flow, source-map skeleton strict parser, planner regression tests, GIT_HISTORY, handoff | `go test ./internal/host/adapt -run 'TestBuildAdaptationProposalVolumeSkeleton\|TestBuildAdaptationProposalRejectsChunkedSkeletonThatShrinksLongTarget\|TestBuildAdaptationProposalRepairsChunkedSkeletonWithoutBatches' -count=1` passed; individual source-map budget/wrapping tests passed; full `go test ./internal/host/adapt -count=1` still hit only Windows `TempDir RemoveAll cleanup: directory is not empty` flakes after assertions. | Planner skeleton generation, proposal revision, detail repair, and missing-chapter repair now receive the configured model-call and structure-repair budgets instead of silently falling back to two structure repairs. Source-map skeleton parsing now requires one strict JSON object with a top-level `batches` array and required batch fields, so high-level prose or locally wrapped single-batch objects are repaired by the model rather than coerced into accepted plans. |
| 2026-07-06 | fix: batch adaptation decision draft generation | fix | Adapt co-create session flow, decision draft batching, co-create prompt context, visible batch events, adapt co-create regression tests, GIT_HISTORY, handoff | `go test ./internal/entry/web -run TestProjectAdaptCoCreate -count=1` passed; `go test ./internal/host -run TestCoCreate -count=1` passed; `git diff --check` passed with CRLF warnings only. | Initial adaptation draft generation now consumes resolved pre-draft decisions in small internal batches instead of exposing all decisions to one model call, and emits visible `第 x/y 批` co-create events. Follow-up normal sends after a draft now call the AI with the current draft in history instead of deterministically appending text, so user refinements are polished into the draft without refreshing the dossier. |
| 2026-07-06 | fix: highlight co-create recommended options | fix | Adaptation co-create decision option rendering, decision option CSS, Web UI tests, rebuilt static assets, GIT_HISTORY, handoff | `cmd /c npm --prefix internal\entry\web\ui test -- --run workspace-progress.test.js styles.test.js` passed; `cmd /c npm --prefix internal\entry\web\ui test -- --run` passed; `cmd /c npm --prefix internal\entry\web\ui run build` passed. | Decision choices now show visible A/B/C/D-style option letters, the skip choice receives the next letter, and the recommended choice is highlighted directly in the option list with a `推荐` chip while remaining unselected until the user clicks it. |
| 2026-07-06 | fix: retry co-create briefing structure | fix | Adapt co-create briefing batch generation/repair retry flow, staged planner retry events, briefing retry regression tests, GIT_HISTORY, handoff | `go test ./internal/host/adapt -run TestEnsureCoCreateBriefing -count=1` passed; `go test ./internal/entry/web -run TestProjectAdaptCoCreate -count=1` passed; `go test ./internal/host/adapt -run "TestPrepareSource.*Dossier\|TestEnsureCoCreateDossier\|TestCoCreateDossier\|TestEnsureCoCreateBriefing" -count=1` passed; `git diff --check` passed with CRLF warnings only. Full `go test ./internal/host/adapt -count=1` and reruns with workspace `TMP` / `-parallel=1` hit only the known Windows `TempDir RemoveAll cleanup: directory is not empty` cleanup failure after assertions. | Co-create briefing batches now retry model-output structure/content failures with `structure_repair_max_attempts`, repair bad JSON or empty-content objects through `buildPlannerRepairPrompt`, regenerate the same missing batch when repair is exhausted, and still reuse already-saved batches on resume. Model-call retries remain governed by `model_call_max_attempts` and briefing/dossier retry events now use their own stages. |
| 2026-07-06 | feat: refine co-create controls and decisions | feat | Co-create max-token config/API/UI, adaptation co-create decision pager with skip/final submit, Web static assets, GIT_HISTORY, handoff | `cmd /c npm --prefix internal\entry\web\ui test -- --run workspace-progress.test.js` passed; `cmd /c npm --prefix internal\entry\web\ui test -- --run` passed; `go test ./internal/entry/web -run 'TestProjectAdaptCoCreate(BeginReturnsAllPendingBriefingDecisions\|BeginWaitsForBriefingDecision\|ResumePreservesFailedDecisionProgress)' -count=1` passed; `go test ./internal/bootstrap ./internal/host ./internal/entry/web -count=1` passed; `cmd /c npm --prefix internal\entry\web\ui run build` passed; `.\restart-web.cmd -Port 9898` passed and restarted PID `31636`; HTTP `/api/runtime` and `/api/models` returned `cocreate_max_tokens=8192`. | Commits the 2026-07-06 replayed co-create max-token work together with the decision UI fix. Adaptation decisions now show one page at a time, support explicit "skip/no modification", require a final submit on the last decision, and allow previous-page review before submit. The update stash remains at `stash@{0}: codex-before-update-20260706-0933` as a backup. |
| 2026-07-06 | fix: preserve loaded novel analysis | fix | Adaptation source resume, novel-library load backup, Web analyze guard, adapt co-create validation, static Web assets, runtime repair notes | `D:\ainovel\.codex\tools\go1.25.5\go\bin\go.exe test ./...` passed; `npm --prefix internal/entry/web/ui test -- --run workspace-progress.test.js` passed; `npm --prefix internal/entry/web/ui run build` passed; `git diff --check` passed with CRLF warnings only; `.\restart-web.cmd -Port 9898 -RuntimeRoot C:\Users\RondleLiu\.ainovel\novels-preview` passed and restarted PID `30172`; HTTP `/api/runtime`, project snapshots, and completed-source analyze no-op returned healthy. | Completed loaded novels no longer restart source analysis when Analyze is clicked. Existing complete reports/foundation can now rebuild missing co-create dossier batches without re-splitting the TXT, project adaptation data is backed up before source snapshot or library-load replacement, and adapt co-create trusts a complete prepared snapshot for the selected source path. Runtime data was repaired: `术士手册` reports 1-946 were backed up and SHA-aligned so the next missing chapter is 947; `女神攻略`, `回档无限`, and `诡案组` project snapshots were restored from the novel library with backups. |
| 2026-07-05 | fix: accept legacy timeline strings | fix | `domain.TimelineEvent` JSON compatibility, import analyzer regression test, world-store timeline regression test, GIT_HISTORY, GIT_NOTES, handoff | `D:\ainovel\.codex\tools\go1.25.5\go\bin\go.exe test ./internal/domain ./internal/host/imp ./internal/store -count=1` passed; `D:\ainovel\.codex\tools\go1.25.5\go\bin\go.exe test ./... -count=1` passed; `.\restart-web.cmd -Port 9898 -RuntimeRoot C:\Users\RondleLiu\.ainovel\novels-preview` passed and restarted PID `57820`; HTTP `/`, `/api/runtime`, and `/api/projects` returned 200; `git diff --check` passed with CRLF warnings only. | Timeline entries serialized as strings now unmarshal as `TimelineEvent{Event: ...}` while object entries keep their full fields. This prevents source-analysis `TIMELINE` output, existing `timeline.json`, and `commit_chapter` append rereads from surfacing `json: cannot unmarshal string into Go value of type domain.TimelineEvent` as a global Web error banner. |
| 2026-07-05 | feat: allow parallel analysis and partial export | feat | Web session action locking, export/simulation/adaptation regression tests, React side-panel busy state, embedded static assets, GIT_HISTORY, GIT_NOTES, handoff | `cmd /c npm --prefix internal\entry\web\ui test -- --run src/workspace-progress.test.js` passed, 1 file / 33 tests; `cmd /c npm --prefix internal\entry\web\ui test -- --run` passed, 6 files / 78 tests; `cmd /c npm --prefix internal\entry\web\ui run build` passed; `D:\ainovel\.codex\tools\go1.25.5\go\bin\go.exe test ./internal/entry/web -run "TestProjectSession(AllowsSimulationDuringAdaptationAnalysis\|AllowsExportDuringAdaptationAnalysis\|RejectsConcurrentResumeContinue\|PauseCancelsAdaptationAnalysis)$" -count=1` passed; `D:\ainovel\.codex\tools\go1.25.5\go\bin\go.exe test ./internal/entry/web ./internal/host ./internal/host/adapt ./internal/host/sim ./internal/host/exp -count=1` passed; `D:\ainovel\.codex\tools\go1.25.5\go\bin\go.exe test ./... -count=1` passed; `git diff --check` passed with CRLF warnings only; `.\restart-web.cmd -Port 9898 -RuntimeRoot C:\Users\RondleLiu\.ainovel\novels-preview` passed and restarted PID `35376`; HTTP `/`, `/api/runtime`, and `/api/projects` returned 200. | Confirms partial export is non-interrupting and exports the current completed chapters while writing or analysis can continue. Adaptation source analysis can now overlap with simulation profile analysis/import from the Web UI; conflicting main writing/proposal actions and duplicate profile actions remain blocked. |
| 2026-07-04 | fix: preview completed chapter revisions | fix | Web completed-chapter preview API, React selected-chapter workspace, embedded static assets, tests, GIT_HISTORY, GIT_NOTES, handoff | `D:\ainovel\.codex\tools\go1.25.5\go\bin\go.exe test ./internal/entry/web` passed; `npm --prefix internal/entry/web/ui test -- --run src/api.test.js src/workspace-progress.test.js` passed; `npm --prefix internal/entry/web/ui run build` passed; `D:\ainovel\.codex\tools\go1.25.5\go\bin\go.exe test ./internal/entry/web ./internal/host/flow ./internal/tools ./internal/store` passed; `.\restart-web.cmd -Port 9898 -RuntimeRoot C:\Users\RondleLiu\.ainovel\novels-preview` passed and restarted PID `40604`; HTTP `/`, `/api/runtime`, and `/api/projects` returned 200; real project `梦中的女孩3` returned complete phase, 59 outline rows, and chapter 1 final content length 6229 via the new API; `git diff --check` passed with CRLF warnings only. | Completed-book single-chapter revision now has a central workspace preview. The UI lazy-loads the selected chapter through `GET /api/projects/{id}/chapters/{chapter}` instead of stuffing all chapter bodies into snapshots, then renders the chosen final/draft content in the main work area while the existing right-pane rewrite/polish controls keep using the same selection state. |
| 2026-07-04 | fix: infer explicit cocreate word count | fix | Web normal co-create intake flow, word-count inference helpers/tests, embedded static assets, GIT_HISTORY, GIT_NOTES | `cmd /c npm --prefix internal\entry\web\ui test -- --run` passed; `cmd /c npm --prefix internal\entry\web\ui run build` passed; `D:\ainovel\.codex\tools\go1.25.5\go\bin\go.exe test ./internal/entry/web -run "TestProjectCoCreatePersistsTargetTotalWords\|TestProjectAdaptCoCreateRejectsTargetTotalWords\|TestProjectCoCreate" -count=1` passed; `D:\ainovel\.codex\tools\go1.25.5\go\bin\go.exe test ./internal/entry/web -count=1` passed; `.\restart-web.cmd -Port 9898 -RuntimeRoot C:\Users\RondleLiu\.ainovel\novels-preview` passed after prepending repo-local Go to PATH and restarted PID `43080`; HTTP `/api/runtime` and `/api/projects` returned 200; `git diff --check` passed with CRLF warnings only. | Normal Web co-create now skips the target-word confirmation only when the initial idea contains an explicit total word count such as `5000字`, `三万字`, `十万字`, or `30w字`. Vague labels such as `长篇` no longer map to a fixed total; they still open the confirmation controls, with structure nudged toward AI judgment while the user chooses the actual total. Per-chapter counts such as `每章5000字` are ignored for total-budget inference. |
| 2026-07-04 | fix: normalize web export file extension | fix | Web export API path normalization, export handler tests, README, GIT_HISTORY, GIT_NOTES | `D:\ainovel\.codex\tools\go1.25.5\go\bin\go.exe test ./internal/entry/web -run TestProjectExport -count=1` passed; `D:\ainovel\.codex\tools\go1.25.5\go\bin\go.exe test ./internal/host/exp -count=1` passed; `D:\ainovel\.codex\tools\go1.25.5\go\bin\go.exe test ./internal/entry/web -count=1` passed; `.\restart-web.cmd -Port 9898 -RuntimeRoot C:\Users\RondleLiu\.ainovel\novels-preview` passed and restarted PID 45780; HTTP `/api/runtime` and `/api/projects` returned 200. | Web export now resolves the selected format before path normalization. A filename without `.txt` / `.epub` gets the selected suffix appended, so `梦中的女孩改` with format `txt` exports as `梦中的女孩改.txt`; explicit `.txt`/`.epub` suffixes that conflict with the selected format are rejected before calling the host export. |
| 2026-07-03 | fix: restore web cocreate action controls | fix | Web co-create React state and suggestion parsing, right-pane draft preview CSS/tests, embedded static assets, GIT_HISTORY, GIT_NOTES | `npm.cmd --prefix internal\entry\web\ui test -- --run` passed; `npm.cmd --prefix internal\entry\web\ui run build` passed; `D:\ainovel\.codex\tools\go1.25.5\go\bin\go.exe test ./internal/entry/web -run TestProjectCoCreate -count=1` passed; `git diff --check` passed with CRLF warnings only; `.\restart-web.cmd -Port 9898` passed and restarted PID 36560; HTTP `/`, `/api/runtime`, and `/api/projects` returned 200. | Web co-create now turns paragraph-style AI choice questions into clickable suggestion buttons, keeps long adaptation draft previews internally scrollable so `启动`/`取消` remain reachable, and honors backend `can_start` so a visible draft is not mislabeled as startable until the latest discussion is consolidated. Playwright MCP browser validation could not run because the transport closed. |
| 2026-07-03 | style: compact project model agent editor | style | Web project model route UI, model route helper text, embedded static assets, style tests, GIT_HISTORY | `npm.cmd --prefix internal/entry/web/ui test -- --run` passed; `go test ./internal/entry/web -run "TestGlobalModel\|TestProjectModel" -count=1` passed; `npm.cmd --prefix internal/entry/web/ui run build` passed; `.\restart-web.cmd -Port 9898` passed and restarted PID 8044; HTTP `/` returned 200; `git diff --check` passed with CRLF warnings only. | Replaces the tall per-agent project model cards with agent chips plus a single provider/model/reasoning editor, preserving role-specific model overrides while making the right pane less crowded. Clarifies the default-inheritance message to mention both model and reasoning overrides. |
| 2026-07-03 | feat: resume proposal runtime and refine model ui | feat | Web model management UI/API usage, embedded static assets, adaptation proposal runtime, co-create retry state, tests, GIT_HISTORY | `go test ./internal/store -run "TestAdaptationStoreSaveProposalClearsProposalRuntime\|TestAdaptationStoreResetGeneratedPreservesSourceSnapshot" -count=1 -v` passed; `go test ./internal/host/adapt -run TestBuildAdaptationProposalResumesChunkedPlannerRuntimeAfterBatchFailure -count=1 -v` passed; `go test ./internal/entry/web -run "TestProjectAdaptCoCreateCommitRetryKeepsFinalDraftAfterProposalFailure\|TestProjectAdaptCoCreateCommitConsolidatesMultiTurnDraftBeforeProposal" -count=1 -v` passed; `cmd /c npm --prefix internal/entry/web/ui test -- --run` passed; `cmd /c npm --prefix internal/entry/web/ui run build` passed; broader Go package runs hit Windows `TempDir RemoveAll cleanup: directory is not empty` flakes after assertions passed; `git diff --check` passed with CRLF warnings only; `.\restart-web.cmd -Port 9898 -NoBuild` passed and restarted PID 4840; HTTP `/` returned 200. | The model pane changes are present in the current worktree alongside this task. Long adaptation proposal generation now persists skeleton/detail runtime checkpoints and clears them on proposal/plan save/reset; adapt co-create commit retries remember final draft consolidation after proposal failure; right-side draft preview expands instead of capping at 220px. |
| 2026-07-02 | fix: persist web cocreate runtime | fix | Web co-create checkpoint/session restore, legacy JSONL restore, configurable model timeout APIs/UI, host co-create timeout logging, embedded static assets, tests, GIT_HISTORY | `go test ./internal/entry/web -run "TestProjectCoCreate(CheckpointRestoresAfterSessionRestart\|RestoresFromLegacyLog\|SuggestionsAndCommitUseDraftPrompt)$"` passed; `go test ./...` passed; `npm.cmd --prefix internal\entry\web\ui test -- --run` passed; `npm.cmd --prefix internal\entry\web\ui run build` passed; `git diff --check` passed with CRLF warnings only; `.\restart-web.cmd -Port 9898 -BindAddress 127.0.0.1 -RuntimeRoot C:\Users\Hi\.ainovel\novels-preview` passed and started PID 35216; HTTP probes confirmed project `project-20260702120450-760715` restores `cocreate.kind=adapt`, `can_start=true`, draft length 1906, source file `梦中的女孩.txt`, and model config timeout 60s. | Adds user-configurable co-create model timeout with a 60s default and project/global persistence, records timeout/model identity in co-create logs, persists active Web co-create sessions to `meta/sessions/web-cocreate-checkpoint.json`, restores co-create state through project snapshots after refresh/restart, migrates old `meta/sessions/cocreate.jsonl` rows into checkpoints when no checkpoint exists, allows starting from any non-empty draft, and forces central co-create workspace output to scroll to the latest stream. |
| 2026-07-02 | fix: stabilize web cocreate completion | fix | Web co-create backend/session state, model diagnostics, React co-create workspace/status, embedded static assets, GIT_HISTORY | `go test ./internal/entry/web ./internal/host -count=1` passed; `go test ./internal/host/adapt -count=1 -parallel=1` passed after the known Windows TempDir cleanup-only flake; `npm.cmd --prefix internal\entry\web\ui test` passed; `npm.cmd --prefix internal\entry\web\ui run build` passed; `.\restart-web.cmd -Port 9898 -BindAddress 127.0.0.1 -RuntimeRoot C:\Users\Hi\.ainovel\novels-preview` passed. | Adapt co-create now keeps the initial user brief, co-create model routing uses visible `architect` and logs selected provider/model, failed co-create sessions can be retried, normal/stage/adapt co-create emits main event feed rows, long central co-create messages scroll and auto-follow streaming output, and Web can start from any non-empty draft even when the model keeps `<ready>false</ready>`. |
| 2026-07-02 | fix: preserve adaptation panel context | fix | Web adaptation/co-create React handlers, embedded static assets, GIT_HISTORY | `npm.cmd test`; `npm.cmd run build`; `go test ./internal/entry/web`; `cmd.exe /c restart-web.cmd -Port 9898` passed. | Preserves window scroll around adaptation mode/start/co-create controls, passes the adaptation direction brief into adaptation co-create as the initial AI input, and hides the `未打开项目` placeholder title when no project is open. |
| 2026-07-02 | fix: improve web adaptation cocreate flow | fix | Web adaptation proposal events, co-create state parsing/UI, right-pane scroll layout, embedded static assets, GIT_HISTORY | `npm.cmd test`; `npm.cmd run build`; `go test ./internal/entry/web`; `cmd.exe /c restart-web.cmd -Port 9898`; static asset probe for `index-97zCu-wk.js` / `index-CCNq7b0J.css` passed. | Proposal generation now emits start/success/failure events so long planner calls are visible. Adaptation co-create suggestions fill the editable input instead of auto-sending, explicit and fallback AI options render as clickable choices, short choices such as keep/reverse/expand gain context, and waiting-state draft controls stay compact. Co-create long assistant text now renders in the central workspace stream area while the right pane keeps mode, suggestions, input, and status visible without page-level scrolling. |
| 2026-07-02 | style: balance web side pane | style | Web UI layout CSS/tests, embedded static assets, GIT_HISTORY | `npm.cmd test -- --run`; `npm.cmd run build`; `go test ./internal/entry/web`; `go test ./...`; `.\restart-web.cmd` all passed. | Rebalanced the desktop Web shell after the right pane became too wide: the workspace now keeps a larger minimum width while the right tool pane remains wider than the old cramped version. Project model route controls now stack role, provider, model, and reasoning selectors in one column so provider/model/inherit controls use matching widths. |
| 2026-07-02 | fix: keep adaptation analysis running after refresh | fix | Web adaptation snapshot/action state, React ADAPT SSE reducer, embedded static assets, regression tests, GIT_HISTORY | `npm.cmd --prefix internal\entry\web\ui test` passed; `npm.cmd --prefix internal\entry\web\ui run build` passed; `go test ./internal/entry/web -count=1` hit a known Windows TempDir cleanup-only flake on the first run, then passed on rerun and passed again after rebuilding static assets; `git diff --check` passed with CRLF warnings only. | Project snapshot responses now report `analysis_status=running` when the same session has an active `adaptation_analysis` action, instead of restoring incomplete disk state as paused. The refreshed React page also consumes `ADAPT` SSE events to move the right-side source-analysis panel through running/done/error even after the original analyze request was lost on browser refresh, and the top Continue action is disabled while analysis is running. |
| 2026-07-02 | fix: restore loaded simulation profile in web snapshots | fix | Web project snapshot simulation status, React simulation restore state, simulation library regression test, embedded static assets, GIT_HISTORY | `go test ./internal/entry/web -run "TestSimulationLibraryUploadSearchAndLoad\|TestProjectSimulateImportUsesExistingHostImportBehavior" -count=1` passed; `npm.cmd --prefix internal\entry\web\ui test` passed; `npm.cmd --prefix internal\entry\web\ui run build` passed; `go test ./internal/entry/web -count=1` hit a known Windows TempDir cleanup-only flake after the build and passed on rerun; `.\restart-web.cmd -Port 9898` passed and restarted Web at `http://127.0.0.1:9898` with PID 41524; HTTP smoke for project `untitled-novel-20260702072655-ac2e9e` returned `simulation.import_status=done`, `imported_file=lhk.json`, and `message=已恢复画像：lhk`. | Project open/snapshot responses now include the loaded simulation profile recovered from `profiles/imported` when `output/meta/simulation_profile.json` exists. The Web UI restores that state into the existing "加载画像" status card after refresh/open, so users can see which library JSON is currently applied. |
| 2026-07-02 | fix: keep web library rows opaque | fix | Web UI library row CSS, CSS regression test, embedded static assets, GIT_HISTORY | `npm.cmd --prefix internal\entry\web\ui test` passed; `npm.cmd --prefix internal\entry\web\ui run build` passed; `go test ./internal/entry/web -count=1` hit a known Windows TempDir cleanup-only flake on the first run and passed on rerun; `.\restart-web.cmd -Port 9898` passed and restarted Web at `http://127.0.0.1:9898` with PID 3080. | Disabled novel-library rows no longer inherit the global disabled opacity while source analysis is running. Library rows now use real grid layout with stable disabled hover styling, so the list stays opaque instead of looking like text is ghosting through the next panel. |
| 2026-07-02 | chore: add one-click web restart script | chore | `restart-web.cmd`, `scripts/restart-web.ps1`, README, GIT_HISTORY | `.\restart-web.cmd -Port 9898` passed, building UI and Go, replacing `ainovel-cli.exe`, restarting, and reporting 2 visible projects; `.\restart-web.cmd -Port 9898 -NoBuild -StopPorts 9898,9901` passed and left Web running as PID 32796; HTTP smoke confirmed `/api/runtime` uses `C:\Users\Hi\.ainovel\novels-preview`, `/api/projects` returns `国中理化课` and `梦中的女孩`, `http://127.0.0.1:9901/` is closed, and `git diff --check` passed with CRLF warnings only. | Adds the single supported development restart path for Web on Windows. The script locates the repo from its own path, builds UI and Go into a temporary executable before stopping the old server, restarts hidden on the selected port, verifies runtime readiness, preserves the preview runtime root when present, and supports portable overrides via `-RuntimeRoot`, `AINOVEL_WEB_RUNTIME_ROOT`, or `AINOVEL_RUNTIME_ROOT`. |
| 2026-07-02 | fix: support global web model management | fix | Global Web model APIs, config/session synchronization, React model panel, runtime action button state, embedded static assets, tests, GIT_HISTORY | `go test ./internal/host ./internal/entry/web -run "TestAddProviderModel\|TestGlobalModel" -count=1`; `npm.cmd test -- --run`; `go test ./internal/bootstrap ./internal/host ./internal/entry/web -count=1` hit known Windows TempDir cleanup-only flakes, then `go test ./internal/host -count=1` and `go test ./internal/entry/web -run "TestGlobalModel\|TestProjectModelAdd\|TestProjectSessionAllowsModelSwitchDuringAction\|TestProjectSessionRejectsConcurrentResumeContinue" -count=1` passed; `npm.cmd run build`; `go build -o .\ainovel-cli.exe .\cmd\ainovel-cli`; HTTP smoke confirmed 9898 200 with `index-CCR4z6hY.js`, `/api/models` returned global routes, and 9901 closed. | The Web model panel can now read and switch global provider/model routes before a project is opened, add a custom proxy globally with the same connectivity test used by project model add, and persist successful global changes for future project sessions. Custom Proxy labels now distinguish provider display name/ID from model name and no longer invent `model-name`. Running projects disable continue/start and composer input while keeping pause available; different projects can still run concurrently via separate browser tabs because the server keeps independent sessions per project. |
| 2026-07-02 | feat: validate and hot-switch web models | feat | Web/host model add and switch flow, model connectivity probe, React model panel busy state, embedded static assets, GIT_HISTORY | `go test ./internal/bootstrap ./internal/host ./internal/entry/web -count=1`; `go test ./internal/host -run TestAddProviderModel -count=1`; `go test ./internal/entry/web -run "TestProjectSessionAllowsModelSwitchDuringAction|TestProjectSessionRejectsConcurrentResumeContinue|TestProjectModelAdd" -count=1`; `npm.cmd test`; `npm.cmd run build`; `go build -o .\ainovel-cli.exe .\cmd\ainovel-cli`; HTTP smoke confirmed `http://127.0.0.1:9898/` 200 with latest JS and 9901 closed. | Web “添加并使用” now performs a short real generation connectivity probe before registering, switching, or persisting a provider/model, so bad base URLs, keys, model names, or relay generation permissions are rejected without changing config. Model switching, thinking changes, and model add/login actions no longer share the project action lock or the global Web busy state, allowing mid-run model changes to affect subsequent LLM calls while current in-flight calls continue on their original model. |
| 2026-07-02 | fix: hide inactive web composer | fix | Web composer render guard, stream round display helper/tests, stream panel CSS, embedded static assets, GIT_HISTORY | `npm.cmd test`; `go test ./internal/entry/web -count=1`; `npm.cmd run build`; `go build -o .\ainovel-cli.exe .\cmd\ainovel-cli`; CDP smoke against `http://127.0.0.1:9898` reported `noProjectComposerCount: 0` and `overlaps: []`; direct `sfkey/deepseek-v4-pro` probes showed `GET /models` 200 but both streaming and non-streaming `POST /chat/completions` returned `429` with `authorization failed` code `11210`. | The bottom composer is no longer rendered before a project is opened, while fresh opened projects still keep the useful quick-start composer. The main stream panel now shows the current effective stream round and uses safer wrapping/scrolling. Closed old 9901/9898 processes, rebuilt static assets before the Go embed build, and restarted the latest executable only on `http://127.0.0.1:9898`; 9901 is closed. The chapter-5 failure is a relay-side generation rejection from sfkey rather than malformed model output or a Web continue-button issue. |
| 2026-07-02 | style: refresh web ui and cocreate workspace | style | React Web UI shell/co-create layout, ChatGPT-inspired CSS refresh, embedded static assets, GIT_HISTORY | `npm.cmd test`; `npm.cmd run build`; `go test ./internal/entry/web -count=1` passed after rerunning known Windows TempDir cleanup flakes; `go build -o .\ainovel-cli.exe .\cmd\ainovel-cli`; CDP screenshot QA at 1920x1080, 1366x768, 390px mobile, and mobile co-create full page with `overlaps: []`. | Restyles the Web UI toward a ChatGPT-like light neutral surface, centered pill composer, low-noise controls, and softer sidebars. Co-create analysis/messages now render in the middle workspace while the right panel keeps mode selection, user input, suggestions, final summary, and launch/cancel actions; the normal bottom composer is hidden during co-create. A preview server was started at `http://127.0.0.1:9901`. |
| 2026-07-02 | feat: add web style catalog and project management | feat | Dynamic style catalog/API, Web project create/style UI, project rename/trash APIs and dialogs, library auto-load, Chinese Web labels, embedded static assets, tests, handoff | `go test ./assets ./internal/entry/web`; `npm.cmd --prefix internal\entry\web\ui test`; `npm.cmd --prefix internal\entry\web\ui run build`; `go build -o .\ainovel-cli.exe .\cmd\ainovel-cli` all passed. Smoke against restarted `http://127.0.0.1:9898` confirmed `/api/styles`, `/api/projects/trash`, and both library list APIs. | Web styles are discovered from `assets/styles/*.md` with labels from the first markdown line and validated before project creation/style changes. Fresh projects can change style and reopened sessions reload the matching prompt/reference bundle. The Web UI restores rename/move-to-trash, adds view/clear recycle bin, auto-loads simulation/novel libraries on app start, and replaces reported English controls with Chinese labels. `D:\AINovel\ainovel-cli.exe` was replaced and restarted with runtime root `C:\Users\Hi\.ainovel\novels-preview`. |
| 2026-07-02 | feat: add web libraries for simulation and adaptation | feat | Web library backend/API, React UI and embedded static assets, seed tooling/docs, adaptation test fixture, style/globalprompt files already dirty in worktree | `go test ./internal/entry/web -count=1`; `go test ./internal/host/adapt ./internal/store -count=1`; `npm.cmd --prefix internal\entry\web\ui test`; `npm.cmd --prefix internal\entry\web\ui run build`; `go build -o .\ainovel-cli.exe .\cmd\ainovel-cli`; `go test ./cmd/seed-web-libraries -count=1` all passed. | Adds `simulation_library` and `novel_library` under the Web runtime root, searchable/selectable Web UI, manifest source-path rewriting on novel save/load, seed tooling for the requested local libraries, and seeded `C:\Users\Hi\.ainovel\novels-preview` while excluding `lhk`. The deployed `D:\AINovel\ainovel-cli.exe` was restarted on port 9898 with the same runtime root. |
| 2026-07-02 | chore: merge latest web updates | chore | Git merge from `github/main`, embedded Web static assets, GIT_HISTORY | `npm.cmd --prefix internal\entry\web\ui test` passed; `npm.cmd --prefix internal\entry\web\ui run build` passed; `go test ./internal/entry/web -count=1` passed on rerun after a Windows `TempDir RemoveAll cleanup` flake; `go build -o .\ainovel-cli.exe .\cmd\ainovel-cli` passed. | Merged `github/main` at `b93370c` into local `main`, preserving the two local adaptation/import fixes while bringing in the latest Web co-create, model admin, Grok OAuth, TUI parity, and refreshed workbench UI updates. |
| 2026-07-01 | fix: disable arc/free word tolerance | fix | startup/TUI/headless adaptation contract, co-create prompts, plan/store normalization, novel context, README/tests | `go test ./internal/entry/startup ./internal/entry/tui ./internal/entry/headless -count=1`; `go test ./internal/host/adapt -run TestBuildAdaptationProposal -count=1`; `go test ./internal/store -run TestAdaptationStoreNormalizesFullRewriteWordTolerance -count=1`; `go test ./internal/tools -run TestContextToolShowsDisabledWordToleranceForFullRewrite -count=1`; `go build -o .\ainovel-cli.exe .\cmd\ainovel-cli` passed. Wider package runs hit known Windows `TempDir RemoveAll cleanup` flakes in store/tools/host/adapt and an unrelated `TestHostCloseWhileWaitDonePendingDoesNotPanic` timeout in `internal/host`. | Keeps `chapter` on `preserve_details + ±15%`, but normalizes `arc/free` to `full_rewrite` with `word_tolerance=disabled`; old full-rewrite plans have residual hard ranges stripped on load, and Writer/co-create context no longer suggests a hard word-count contract for arc/free. |
| 2026-07-01 | fix: split parenthesized source chapters | fix | import splitter, splitter tests, GIT_HISTORY | `go test ./internal/host/imp -run TestSplitText_ParenthesizedChinese -count=1 -v`; `AINOVEL_MZDNH_FIXTURE=D:\AINovel\novel\mzdnh\mzdnh.txt go test ./internal/host/imp -run TestSplitFile_ExternalMzdnhFixture -count=1 -v`; `go test ./internal/host/imp -run TestSplit -count=1`; `go test ./internal/host/imp -count=1`; `go build -o .\ainovel-cli.exe .\cmd\ainovel-cli` passed. | Supports source files whose chapter headings are standalone Chinese parenthesized numerals such as `（一）`/`（二）`, including source-site metadata lines that trail into headings like `精华积分N/A帖子阅读权限注册N/A（八）`. Parenthesized markers are ignored when standard `第N章` headings already exist, so in-chapter sections are not split. `D:\AINovel\novel\mzdnh\mzdnh.txt` now splits into 17 chapters and was exported to `D:\AINovel\novel\mzdnh\chapters_txt`. |
| 2026-07-01 | style: refresh web workbench UI | style | React Web UI shell/styles, embedded static assets, GIT_HISTORY | `npm test` passed; `npm run build` passed; `go build -o .\ainovel-cli.exe .\cmd\ainovel-cli` passed using project-local Go 1.25.5; Playwright checked the embedded backend preview at 1366x768, 1920x1080, 1180x768, 960x900, and 600x900 with no horizontal overflow or console errors. | Restyles the Web UI into a Windows 11 Fluent-inspired workbench: quieter light palette, Segoe/Microsoft YaHei font stack, compact right inspector tabs, fixed-height command bar, calmer empty state, and responsive stacking for narrow Windows snap layouts. |
| 2026-07-01 | fix: recognize arc review checkpoints | fix | `internal/host/flow` state/test, `internal/host/resume`, GIT_HISTORY, rebuilt exe | `go test ./internal/host/flow ./internal/host/reminder -count=1` passed; `go test ./internal/tools ./internal/host -run 'TestSaveReview\|TestCommitChapter\|TestReview\|TestResume\|TestCheckpoint\|TestRoute' -count=1` passed on rerun after a known Windows `TempDir RemoveAll cleanup` flake; `go build -o .\ainovel-cli.exe .\cmd\ainovel-cli` passed. | Arc-end routing and resume labels now treat an arc-scoped `review` checkpoint as completed even when the saved review artifact is named after a different source/original chapter. This prevents layered adaptation projects like `nsgl` from repeatedly dispatching the same arc review after the editor already accepted it. |
| 2026-07-01 | fix: retry structured parse failures | fix | `internal/host/imp` structured call retry/tests, GIT_HISTORY, rebuilt exe | `go test ./internal/host/imp ./internal/host/adapt -count=1` passed on rerun; first attempt hit a transient Windows `TempDir RemoveAll cleanup` flake in `internal/host/adapt`. `go build -o .\ainovel-cli.exe .\cmd\ainovel-cli` passed; `D:\AINovel\ainovel-cli.exe` was synced to the rebuilt SHA256, while `C:\Users\Hi\go\bin\ainovel-cli.exe` stayed old because active CLI processes held the file. | Structure/envelope parse errors such as missing analyzer tags or malformed JSON now keep using the configured 7-attempt retry budget with corrective feedback instead of stopping after one format retry, so adaptation source preparation can recover from intermittent bad model output the same way a manual continue often does. |
| 2026-07-01 | fix: split bracket-prefixed source chapters | fix | import splitter, splitter tests, README, GIT_HISTORY | `go test ./internal/host/imp -run TestSplit -count=1`; `AINOVEL_NSGL_FIXTURE=D:\AINovel\novel\nsgl\nsgl.txt go test ./internal/host/imp -run TestSplitFile_ExternalNsglFixture -count=1 -v`; `go test ./internal/host/imp -count=1`; `AINOVEL_GAZ_FIXTURE=D:\AINovel\novel\gaz\gaz.txt go test ./internal/host/imp -run TestSplitFile_ExternalGazFixture -count=1 -v`; `go build -o .\ainovel-cli.exe .\cmd\ainovel-cli` passed, and `D:\AINovel\ainovel-cli.exe` was synced to the rebuilt binary. | Supports source headings like `【女神攻略】第二章 目睹` and compact `【女神攻略】第九章银屏女神`, treats middle-dot title separators as valid, and collapses duplicate source-site headings when only metadata such as author/date separates them from the real chapter title. `D:\AINovel\novel\nsgl\nsgl.txt` now splits into 17 chapters instead of 2. |
| 2026-07-01 | fix: resume simulate progress | fix | `internal/host/sim` runner/retry/tests, GIT_HISTORY | `go test ./internal/host/sim -count=1` passed; `go test ./internal/entry/tui ./cmd/ainovel-cli -count=1` passed; `go test ./internal/host/... ./internal/entry/tui ./cmd/ainovel-cli -count=1` passed; `go build -o .\ainovel-cli.exe .\cmd\ainovel-cli` passed and PATH executable `C:\Users\Hi\go\bin\ainovel-cli.exe` was synced to matching SHA256. One first sim-test attempt hit a transient Windows `TempDir RemoveAll cleanup` flake, then reran cleanly. | `/simulate` now saves each successful source report into `meta/simulation_profile.json` before moving on, resumes from the first unfinished/changed source instead of reanalyzing completed files, can resume synthesis-only after a merge failure, and retries malformed JSON once with feedback plus transient model/network failures using the shared retry policy. |
| 2026-07-01 | docs: clarify adaptation mode selection | docs | README | `rg -n -e "chapter => preserve_details" -e "只选择" -e "两步固定选择" -e "第二步选择" README.md internal\entry\tui\adapt_confirm.go internal\entry\tui\cocreate.go internal\host\cocreate.go` confirmed README matches fixed policy code and old two-step policy wording is gone. | Clarifies the real TUI flow: after source analysis the user chooses only `chapter/arc/free`; rewrite policy is fixed by granularity (`chapter => preserve_details`, `arc/free => full_rewrite`), then adaptation co-create is used to choose the concrete adaptation direction. |
| 2026-06-30 | fix: infer review issue chapters | fix | `save_review` issue severity gate/tests, GIT_HISTORY | `go test ./internal/tools -run "TestSaveReviewIssue\|TestInferAffectedChaptersFromIssues\|TestSaveReviewDerivesVerdictFromScore\|TestPreserveDetails\|TestCommitChapterAdaptationFinalChapterWaitsForReview\|TestCommitChapterLayeredWaitsForReviewWhenDone\|TestCommitChapterRequiresPassingAdaptationCheck" -count=1`; `go test ./internal/host/reminder -run "TestSubAgentGuard\|TestStopGuard_DefaultBlockMessage" -count=1`; `go test ./internal/host/flow -run "TestRoute_AdaptationCompleteNeedsCompleteBook\|TestRoute_NeedsNewVolume\|TestRoute_ArcEnd" -count=1`; `go build -o .\ainovel-cli.exe .\cmd\ainovel-cli` passed; repo/global/upper-level executable hashes match. | When Editor issue severity upgrades an arc/global `accept` to polish/rewrite but the model omitted `affected_chapters`, infer chapters from issue text such as `第3章` or `chapter 3` before falling back to the review chapter. This avoids polishing the wrong arc-end chapter when the issue evidence points elsewhere. |
| 2026-06-30 | fix: prevent preserve-details adaptation loops | fix | preserve-details adaptation check/commit gates, Writer stop guard, Editor review severity gate, adaptation writer prompt, flow router/state, README/tests | `go test ./internal/tools -run "TestSaveReviewIssue\|TestSaveReviewDerivesVerdictFromScore\|TestPreserveDetails\|TestCommitChapterAdaptationFinalChapterWaitsForReview\|TestCommitChapterLayeredWaitsForReviewWhenDone\|TestCommitChapterRequiresPassingAdaptationCheck" -count=1`; `go test ./internal/host/reminder -run "TestSubAgentGuard\|TestStopGuard_DefaultBlockMessage" -count=1`; `go test ./internal/host/flow -run "TestRoute_AdaptationCompleteNeedsCompleteBook\|TestRoute_NeedsNewVolume\|TestRoute_ArcEnd" -count=1`; `go build -o .\ainovel-cli.exe .\cmd\ainovel-cli` passed. Headless acceptance on `D:\tavo\命案\命案_切分目录\命案_001_章节001-006_13863字.txt` with `chapter + preserve_details` completed 6 chapters, ran Writer checks, entered Editor review/arc summary/volume summary, then `complete_book`. Full combined package test only hit known Windows `TempDir RemoveAll cleanup` flakes in `internal/tools`; affected focused tests passed when run directly. | Fixes the empty `change_evidence` dead loop, makes preserve-details repair instructions point to the right tool argument instead of word-count repair, prevents final adaptation chapters from skipping Editor, finishes adapted books with `complete_book` after summaries, and makes `save_review` escalate `error`/`critical` issues even if the model says `accept`. Repo, global PATH, and upper-level executables were rebuilt to matching hashes. |
| 2026-06-30 | fix: bound TUI frame height | fix | TUI top-level view framing, done-state layout regression test, README | `go test ./internal/entry/tui -run 'TestViewFitsTerminalHeightInDoneMode\|TestRenderInputBoxClampsInputViewToOneLine\|TestCoCreateBodyClampsInputViewToOneLine' -count=1`; `go test ./internal/entry/tui ./cmd/ainovel-cli -count=1`; `go test ./internal/retrypolicy ./internal/host ./internal/host/imp ./internal/host/adapt ./internal/userrules ./internal/agents ./internal/entry/tui -count=1` passed using `C:\Program Files\Go\bin\go.exe`. | Ensures every TUI view frame is padded/truncated to the current terminal size so completed/export/co-create redraws cannot leave repeated stale prompts in terminal scrollback. |
| 2026-06-30 | fix: scope adaptation writer guidance | fix | adaptation writer prompt asset, asset loader, novel context references/builders/tests | `go test ./assets ./internal/tools -run 'TestLoadKeepsAdaptationGuidanceOutOfBaseWriterPrompt\|TestContextToolInjectsAdaptationWriterGuidanceOnlyForAdaptation'` passed using `C:\Program Files\Go\bin\go.exe`. Wider `go test ./assets ./internal/tools` passed for `assets` and hit an existing Windows TempDir cleanup failure in `internal/tools`. | Adds an adaptation-only writer guidance file for `chapter + preserve_details`, forbids parenthesized patch labels such as inner-monologue tags, and injects the guidance only when `novel_context` is serving an adaptation chapter so original writing prompts are unaffected. |
| 2026-06-30 | fix: stabilize retries and TUI input rendering | fix | retry policy, co-create/structured/user-rule retry paths, TUI co-create and bottom input rendering, README/tests | `go test ./internal/retrypolicy ./internal/host ./internal/host/imp ./internal/host/adapt ./internal/userrules ./internal/agents ./internal/entry/tui -count=1`; `go test ./internal/entry/startup ./internal/entry/headless ./internal/entry/tui ./cmd/ainovel-cli -count=1`; `go build -o .\ainovel-cli.exe .\cmd\ainovel-cli` passed using `C:\Program Files\Go\bin\go.exe`; repo/global executable hashes match. | Uses a shared 7-attempt retry policy with increasing delays for transient model/network failures, makes co-create failures visible and retryable from TUI, and clamps TUI input rendering to one line so completed/export/co-create states no longer flood the terminal with repeated placeholder lines. |
| 2026-06-30 | fix: repair preserve-details short draft loop | fix | writer prompt, plan/draft/check/commit adaptation guidance, writer stop guard, focused tests | `go test ./internal/tools -run 'TestPreserveDetails\|TestAdaptationWriterToolsReject\|TestCommitChapterRequiresPassingAdaptationCheck'`; `go test ./internal/host/reminder -run 'TestSubAgentGuard'`; `go test ./internal/tools -run 'TestDraftChapterRejectsUnexpandedLayeredChapter\|TestCommitChapterRequiresPassingAdaptationCheck' -count=1`; `go test ./internal/host/reminder -count=1`; `go test ./internal/agents ./internal/host/reminder`; `go build -o .\ainovel-cli.exe .\cmd\ainovel-cli` passed using `C:\Program Files\Go\bin\go.exe`; repo/global executable hashes match. Full `go test ./internal/tools ./internal/host/reminder` still hit known Windows TempDir cleanup flakes in `internal/tools` only. | Fixes the `preserve_details` failure mode where writer wrote a short changed fragment then repeatedly tried `commit_chapter`; plan/draft/check/commit now steer the model to read source and write a complete source-backed chapter before commit, and stop guard tells writer to repair short drafts instead of blindly committing. |
| 2026-06-30 | fix: choose adaptation mode before cocreate | fix | TUI adaptation preparation/mode selection/co-create startup, adaptation defaults, README | `go test ./internal/entry/startup ./internal/entry/tui ./internal/entry/headless ./internal/host ./internal/host/adapt ./internal/host/flow ./cmd/ainovel-cli`; `go build -o .\ainovel-cli.exe .\cmd\ainovel-cli` passed using `C:\Program Files\Go\bin\go.exe`; repo/global executable hashes match. `go test ./internal/tools` hit existing Windows TempDir cleanup failures only. | Moves `chapter/arc/free` and `full_rewrite/preserve_details` selection before adaptation co-create, passes the selected contract into the co-create opener and final plan, removes the post-co-create mode confirmation, and makes default adaptation policy `chapter + preserve_details + ±15%`. |
| 2026-06-30 | fix: retry cocreate stream EOF | fix | host co-create stream retry/tests | `go test ./internal/host -run TestCoCreateStream -count=1`; `go test ./internal/host -count=1`; `go test ./internal/entry/startup ./internal/entry/tui -run CoCreate -count=1`; `go build -o .\ainovel-cli.exe .\cmd\ainovel-cli` passed using `C:\Program Files\Go\bin\go.exe`; repo/global executable hashes match. | Retries transient co-create stream failures such as unexpected EOF/network/idle errors up to three attempts, clears partial co-create preview before retry, and avoids retrying user cancellation. |
| 2026-06-30 | fix: normalize grok reasoning history | fix | globalprompt model wrapper/tests | `go test ./internal/globalprompt`; `go test ./internal/globalprompt ./internal/bootstrap ./internal/grokauth ./internal/host ./internal/entry/tui`; `go build -o .\ainovel-cli.exe .\cmd\ainovel-cli` passed using `C:\Program Files\Go\bin\go.exe`; repo/global executable hashes match. | Converts Grok/xAI thinking history before provider calls so compat backends no longer reject resumed writing with `ReasoningBlock history is not supported by this compat provider`. |
| 2026-06-30 | fix: accept xai grok bare login codes | fix | grokauth manual completion, TUI Grok login hint, grokauth tests | `go test ./internal/grokauth ./internal/entry/tui`; `go build -o .\ainovel-cli.exe .\cmd\ainovel-cli` passed using `C:\Program Files\Go\bin\go.exe`. | Matches xAI's Grok Build page that returns a one-time code instead of a loopback callback URL; bare codes are accepted only while bound to the current in-memory PKCE login session. |
| 2026-06-30 | feat: add runtime model onboarding and grok oauth | feat | bootstrap provider config/modelset, grokauth store/login, Host model registration, TUI `/model add`, globalprompt Grok prompt, docs/tests | `go test ./internal/globalprompt ./internal/bootstrap ./internal/grokauth ./internal/host ./internal/entry/tui`; `go build ./cmd/ainovel-cli` passed using `C:\Program Files\Go\bin\go.exe`. | Adds post-initialization model adding from TUI, custom proxy setup, Grok OAuth PKCE login with local token store, runtime ModelSet registration/persistence, and Grok-specific global prompt selection. |
| 2026-06-30 | feat: add controllable adaptation contracts | feat | adaptation domain/store/runner, host/headless/TUI startup, writer gates/tools/router/context, prompts, focused tests | `go test ./internal/domain ./internal/store ./internal/host/adapt ./internal/tools ./internal/host/flow ./internal/entry/startup ./internal/entry/headless ./internal/entry/tui ./cmd/ainovel-cli`; `go test ./internal/host ./internal/agents` passed using `C:\Program Files\Go\bin\go.exe`; `git diff --check -- . ':!internal/globalprompt/global-prompt-grok.md'` passed. | Adds explicit `granularity` and `rewrite_policy` adaptation contracts, proposal vs confirmed plan storage, preserve-details word ranges, TUI mode confirmation, headless flags, writer/router/tool gates, adaptation context fields, and prompt policy split between full rewrite and source-detail preservation. |
| 2026-06-30 | fix: support gaz source chapter splitting | fix | import splitter, splitter tests, README | `go test ./internal/host/imp -run TestSplit`; `AINOVEL_GAZ_FIXTURE=D:\AINovel\gaz\gaz.txt go test ./internal/host/imp -run TestSplitFile_ExternalGazFixture -v`; `go test ./internal/host/imp ./internal/host/adapt` passed using `C:\Program Files\Go\bin\go.exe`. Full `go test ./...` still fails in unrelated Windows/environment cases: TempDir cleanup flakes, missing `sh` for notify tests, and executable mode expectations in version tests. | Adds volume-prefixed chapter headings, old-style Chinese `十一章`/`廿一章`, inline trailing heading recovery, and non-story boundary filtering for preview/lingyi-dangan/editor-note sections so `gaz.txt` splits into 237 adaptation source chapters while preserving existing formats. |
| 2026-06-30 | feat: stabilize adaptation source preparation | feat | adaptation runner/store/domain, import analyzer structured call helpers, merge prompt/assets, focused tests, default style | `go test ./internal/host/adapt ./internal/host/imp ./internal/store ./assets`; `go test ./internal/host ./internal/entry/tui` passed using `C:\Program Files\Go\bin\go.exe`. | Replaces one-shot source `ReverseFoundation` in adaptation preparation with per-chapter report persistence, SHA-based resume, streamed/retried structured calls, report-based foundation merge, deterministic source outline generation, architect failover, and includes the requested `assets/styles/default.md` update. |
| 2026-06-29 | fix: auto-run simulate from command palette | fix | TUI command registry/tests | `go test ./internal/entry/tui` passed using `C:\Program Files\Go\bin\go.exe`. | Makes `/simulate` execute immediately when accepted from the slash-command palette; the command has no args, so requiring a second Enter made it look like simulate did nothing. |
| 2026-06-29 | fix: log adaptation preparation failures | fix | TUI adaptation preparation | `go test ./internal/entry/tui` passed using `C:\Program Files\Go\bin\go.exe`. | Writes complete source-analysis errors to `output/novel/logs/tui.log` so provider HTTP failures are not lost behind the modal width. |
| 2026-06-29 | fix: tolerate UTF-8 BOM in config files | fix | bootstrap config file loader/tests | `go test ./internal/bootstrap` passed using `C:\Program Files\Go\bin\go.exe`. | Strips UTF-8 BOM before JSON comment removal/unmarshal so Windows-written config files no longer break startup with `invalid character 'ï'`. |
| 2026-06-29 | fix: persist model switch candidates | fix | bootstrap config/setup, host model switching, bootstrap tests | `go test ./internal/bootstrap`; `go test ./internal/entry/tui`; `go test ./internal/host` passed using `C:\Program Files\Go\bin\go.exe`. | Keeps previously selected provider/model pairs in `providers.<name>.models` so switching from DeepSeek to GPT does not make DeepSeek disappear from `/model`. |
| 2026-06-29 | feat: select global prompt by model family | feat | internal/globalprompt, bootstrap model creation, `/model` UI, assets docs, bootstrap config tests | `go test ./internal/entry/tui ./internal/globalprompt ./internal/bootstrap ./assets`; `go test ./internal/globalprompt ./internal/bootstrap ./assets ./internal/agents ./internal/userrules ./internal/host/imp ./internal/host/sim ./internal/host/adapt` passed; `go build -o ainovel-cli.exe ./cmd/ainovel-cli` passed. | Splits the embedded global prompt into DeepSeek and GPT templates, injects the selected template at the LLM model boundary so `/model` switches follow the active model, shows option counts in `/model`, and fixes Windows test home isolation. |
| 2026-06-29 | feat: show adaptation preparation progress | feat | TUI adaptation startup modal, adaptation store cleanup, host startup, tests | `go test ./internal/host/adapt ./internal/entry/tui ./internal/host` passed; `go test ./internal/store -run TestAdaptationStore` and `go test ./internal/host/adapt -run TestPrepareRunWorksAfterResetGenerated` passed. Full `go test ./internal/store` hit existing Windows `t.TempDir` cleanup failures (`directory is not empty`) in unrelated tests. | Shows chapter-level source analysis progress before adaptation co-create and preserves source snapshots when starting confirmed adaptation runs. |
| 2026-06-29 | fix: avoid default chapter length for external novels | fix | user rules defaults, import/adaptation startup paths, tests | `go test ./internal/rules ./internal/userrules`; `go test ./internal/entry/tui ./internal/entry/headless ./internal/host` passed. Full `go test ./...` still fails in unrelated bootstrap/notify/tools/version Windows/environment cases. | Keeps anti-AI mechanical rules for imported/adapted novels while omitting the original-writing 3000-6000 default chapter length unless explicitly set by the user. |
| 2026-06-29 | fix: harden import text handling | fix | import prompts, import parser/analyzer/foundation, text encoding utils, global prompt template | `go test ./internal/host/imp ./internal/utils ./internal/globalprompt ./assets ./internal/userrules`; `go build ./cmd/ainovel-cli` passed. | Improves external novel import stability with prompt-side JSON discipline, tolerant JSON extraction, UTF-8 cleanup, UTF-16/GBK decoding tests, and the current customized global prompt template. |
| 2026-06-29 | feat: add global prompt prefix | feat | internal/globalprompt, assets prompt loading, agent/co-create/user-rules prompt wiring | `go test ./internal/globalprompt ./assets ./internal/userrules ./internal/host/... ./internal/agents/...`; `go vet` same scope; `go build ./cmd/ainovel-cli` passed. Full `go test ./...` still fails in bootstrap/notify/version due existing Windows assumptions. | Adds a replaceable global system prompt template and idempotent injection across built-in prompt assets, coordinator/subagents, co-create, user-rules normalizer, and writer summary system prompt. |
| 2026-06-29 | feat: add novel adaptation mode | feat | startup/headless/TUI/host/tools/store/domain/docs/tests | Focused adaptation and entry tests passed; full `go test ./...` still fails in bootstrap/notify/version due existing Windows/config assumptions | Adds source snapshot storage, adaptation planning, writer tools, commit gate, TUI/headless entry, and README docs. |
